{
  "name": "travelai-planner",
  "version": "0.2.0",
  "private": true,
  "description": "TravelAI Planner React + Supabase SaaS application shell.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "test": "npm run test --prefix client -- --run",
    "test:e2e": "npm run test:e2e --prefix client",
    "audit": "npm audit --audit-level=moderate --prefix client",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
