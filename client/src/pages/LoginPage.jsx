import PlaceholderPage from '../components/PlaceholderPage.jsx';

function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      description="Login screen shell ready hai. Real JWT authentication and form submission Milestone 3 mein implement hoga."
      plannedItems={['Email input placeholder', 'Password input placeholder', 'Dashboard redirect after future auth']}
    />
  );
}

export default LoginPage;
