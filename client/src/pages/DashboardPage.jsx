import PlaceholderPage from '../components/PlaceholderPage.jsx';

function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="User dashboard shell ready hai. Saved trips, stats, and personalized data later milestones mein backend se connect honge."
      plannedItems={['Quick actions', 'Recent trips', 'AI usage summary', 'Recommended destinations']}
    />
  );
}

export default DashboardPage;
