import PlaceholderPage from '../components/PlaceholderPage.jsx';

function DestinationsPage() {
  return (
    <PlaceholderPage
      title="Destination Discovery"
      description="Destination discovery shell ready hai. Filters, destination data, and recommendations later milestones mein add honge."
      plannedItems={['Search bar', 'Budget filter', 'Season filter', 'Destination cards']}
    />
  );
}

export default DestinationsPage;
